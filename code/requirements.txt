# Core (CPU replay, runs on any machine)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
datasets>=2.16

# Apple Silicon inference (M4/M-series Mac)
# pip install mlx-lm
# mlx-lm>=0.19

# GPU inference (cloud only)
# torch>=2.1
# transformers>=4.36
# accelerate>=0.25

# Agents domain
# alfworld>=0.3
# pyyaml>=6.0
